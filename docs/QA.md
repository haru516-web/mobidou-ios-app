# MVP検証記録 — 2026-09-09

## 自動検証

- TypeScript `tsc --noEmit` 成功。
- 進行ルールの9テスト成功（`npm test`）。999/1,000歩の境界、3種まとめて解放、日付変更、未取得の続きから再開、6種完成後の重複なし、歩数訂正、JSON保存復元、不正保存形式、体験データの独立性。
- Expo SDK54の依存バージョンチェック成功（オフラインの同梱バージョン表によるチェック）。
- Webの本番バンドル書き出し成功。
- iOSのHermesバンドル書き出し成功。これはSwiftのコンパイル・実機ビルドの証明ではありません。
- Expo autolinkingでローカル `MobiHealthModule` / `MobiHealth.podspec` を検出。
- Expo config introspectionでHealthKit entitlement、NSHealthShareUsageDescription、NSMotionUsageDescriptionを確認。結果は `expo-config-check.json`。
- ペット26ファイルを元の `D:\english-ios-app\assets\mobies` とSHA256比較し、全件一致。
- 御朱印6画像をすべて目視。片目レンズ、十字キー、丸ボタン2つと創作社名を確認。
- 日本語フォントを2ウェイト、アイコンをIoniconsのみに絞り、書き出し対象アセットを56から35へ削減。

## ブラウザによる操作確認

Codexブラウザ上の http://localhost:8083 にて実施。390×844 と375×667の表示を確認。

1. 初回案内から体験モードへ遷移。
2. モビ坊をなでると「へへっ、そこそこ！」と反応、ふれあい回数が1に増加。
3. 体験1,000歩で星の御朱印の授与画面を表示。御朱印帳にしまうと1/6印。
4. 「集めた (1)」で取得済み1件に絞り込み。
5. 再読み込みで1,000歩と取得状態を復元。済んだ授与演出は再表示なし。
6. 26体の選択ボタンを確認。ウルフモビーに変更し、ホームも切り替わることを確認。
7. ウルフモビーに話しかけると「急がなくていい。隣にいる。」。
8. 3,000歩で月の御朱印を取得。375×667で授与画面のボタンまで操作できることを確認。
9. 翌日のめぐりを体験すると歩数0へ。取得した星・月は保持され、未取得の雨が1,000歩の目標になる。
10. 設定からプライバシー案内を表示・閉じる操作を確認。
11. 体験を終了すると実記録は0/6のまま。体験の2印が実記録に混ざらない。
12. Webで歩数を連携するとiPhoneで利用する案内を表示。設定画面内でも読めることを確認。
13. ブラウザのconsole errorは0件。

## 実機で残る確認

- Mac/XcodeでのSwiftコンパイル、Apple署名、iPhoneへのインストール。
- HealthKitの権限承認・拒否・取り消しと実際の歩数。
- Expo Go / Core Motionの実歩数。
- iPhone上の振動とReduce Motion。
- アプリをバックグラウンドにした状態から翌日に復帰する実機試験。
- 通信を切ったインストール済み本番ビルドでの起動・保存・歩数取得。

AndroidのHealth ConnectとApp Store/TestFlightへの配信は今回実施していません。
